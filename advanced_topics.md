#Advanced Topics

##Branching

Each git branch represents a git object that points to the last branch's commit (commit object, see [internals.md#object-files](this)). 
That is why it is very cheap operation in git to create a new branch.
